# BC Flame Traefik Deployment Environment Variables
# Copy this to .env.traefik and fill in with actual values

# Database
DB_USER=bcflame
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=bcflame_db

# Strapi Secrets (GENERATE NEW VALUES - see commands below)
JWT_SECRET=
ADMIN_JWT_SECRET=
APP_KEYS=
API_TOKEN_SALT=
TRANSFER_TOKEN_SALT=

# Domain Configuration
# For localhost development
# FRONTEND_DOMAIN=bcflame.localhost
# API_DOMAIN=api.bcflame.localhost

# For production (update with your actual domain)
FRONTEND_DOMAIN=bcflame.com
API_DOMAIN=api.bcflame.com

# CORS Origins (comma-separated, no spaces)
# Must match your domains exactly
CORS_ORIGINS=https://bcflame.com,https://www.bcflame.com,https://api.bcflame.com

# Frontend URLs (used during build)
NEXT_PUBLIC_STRAPI_URL=https://api.bcflame.com
NEXT_PUBLIC_SITE_URL=https://bcflame.com

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM_NAME=BC Flame
EMAIL_FROM_ADDRESS=noreply@bcflame.com
EMAIL_ADMIN_RECIPIENTS=admin@bcflame.com

# Database Seeding
# Set to 'true' only for first deployment to seed initial data
SEED_DATA=false

# ============================================
# COMMANDS TO GENERATE SECRETS
# ============================================
# Run these commands and paste the output above:
#
# JWT_SECRET:
# openssl rand -base64 32
#
# ADMIN_JWT_SECRET:
# openssl rand -base64 32
#
# APP_KEYS (4 comma-separated keys):
# echo "$(openssl rand -base64 16),$(openssl rand -base64 16),$(openssl rand -base64 16),$(openssl rand -base64 16)"
#
# API_TOKEN_SALT:
# openssl rand -base64 32
#
# TRANSFER_TOKEN_SALT:
# openssl rand -base64 32
