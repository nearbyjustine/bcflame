# BC Flame Traefik Deployment Environment Variables
# Copy this to .env.traefik and fill in with actual values

# Database
DB_USER=bcflame
DB_PASSWORD=bcflame
DB_NAME=bcflame_db

# Strapi Secrets (GENERATE NEW VALUES - see commands below)
JWT_SECRET=Ge6L57axEJe/a9feZbJR8x/gzFYp+4wXJvo5EIaOlF8=
ADMIN_JWT_SECRET=/SzQimXi1YyT0uDbudOWocrTlUie1CFukOGRJvyAAj0=
APP_KEYS=3/8d8cyJC+Q6gnDy829rCA==,RcFZJ/I1Jo6sIVfRqJYHZQ==,7ez1Zly6tjwvMre0wBdulQ==,VhRsN0y72CTtGrsOmdrO3w==
API_TOKEN_SALT=ldvQXvjrvB64mzMhTBXrfh8KpJyiqIszbFDIR3SnPvo=
TRANSFER_TOKEN_SALT=sbfZ/FMgJF4kbauBBKqlXdB9s6/aZhbvA0qaArAO+Yk=

# Domain Configuration
# For localhost development
# FRONTEND_DOMAIN=bcflame.localhost
# API_DOMAIN=api.bcflame.localhost

# For production on hows-tine.com (update subdomain as needed)
FRONTEND_DOMAIN=bcflame.hows-tine.com
API_DOMAIN=api.bcflame.hows-tine.com

# CORS Origins (comma-separated, no spaces)
# Must match your domains exactly
CORS_ORIGINS=https://bcflame.hows-tine.com,https://www.bcflame.hows-tine.com,https://api.bcflame.hows-tine.com

# Frontend URLs (used during build)
NEXT_PUBLIC_STRAPI_URL=https://api.bcflame.hows-tine.com
NEXT_PUBLIC_SITE_URL=https://bcflame.hows-tine.com

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=nearbyjustine@gmail.com
SMTP_PASS=Euphel!0996724682
EMAIL_FROM_NAME=BC Flame
EMAIL_FROM_ADDRESS=nearbyjustine@gmail.com
EMAIL_ADMIN_RECIPIENTS=nearbyjustine@gmail.com

# Database Seeding
# Set to 'true' only for first deployment to seed initial data
SEED_DATA=true

# ============================================
# COMMANDS TO GENERATE SECRETS
# ============================================
# Run these commands and paste the output above:
#
# JWT_SECRET:
# openssl rand -base64 32
#
# ADMIN_JWT_SECRET:
# openssl rand -base64 32
#
# APP_KEYS (4 comma-separated keys):
# echo "$(openssl rand -base64 16),$(openssl rand -base64 16),$(openssl rand -base64 16),$(openssl rand -base64 16)"
#
# API_TOKEN_SALT:
# openssl rand -base64 32
#
# TRANSFER_TOKEN_SALT:
# openssl rand -base64 32
