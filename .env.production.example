# BC Flame Production Environment Variables
# Copy this to .env.production and fill in with actual values

# Database
DB_USER=bcflame
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=bcflame_db

# Strapi Secrets (GENERATE NEW VALUES - see commands below)
JWT_SECRET=
ADMIN_JWT_SECRET=
APP_KEYS=
API_TOKEN_SALT=
TRANSFER_TOKEN_SALT=

# CORS Origins (comma-separated, no spaces)
# Update with your actual domain
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Frontend URLs
# Update with your actual domain
NEXT_PUBLIC_STRAPI_URL=https://api.yourdomain.com
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM_NAME=BC Flame
EMAIL_FROM_ADDRESS=noreply@yourdomain.com
EMAIL_ADMIN_RECIPIENTS=admin@yourdomain.com

# Database Seeding
# Set to 'true' only for first deployment to seed initial data
SEED_DATA=false

# ============================================
# COMMANDS TO GENERATE SECRETS
# ============================================
# Run these commands and paste the output above:
#
# JWT_SECRET:
# openssl rand -base64 32
#
# ADMIN_JWT_SECRET:
# openssl rand -base64 32
#
# APP_KEYS (4 comma-separated keys):
# echo "$(openssl rand -base64 16),$(openssl rand -base64 16),$(openssl rand -base64 16),$(openssl rand -base64 16)"
#
# API_TOKEN_SALT:
# openssl rand -base64 32
#
# TRANSFER_TOKEN_SALT:
# openssl rand -base64 32
