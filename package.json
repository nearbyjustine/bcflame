{
  "name": "bcflame",
  "version": "1.0.0",
  "description": "BC Flame Premium Client Portal - Monorepo",
  "private": true,
  "scripts": {
    "db:setup": "docker-compose up -d postgres",
    "db:start": "docker start bcflame_postgres",
    "db:stop": "docker stop bcflame_postgres",
    "db:reset": "docker rm -f bcflame_postgres && docker volume rm bcflame_postgres_data && npm run db:setup",
    "dev:backend": "cd backend && npm run develop",
    "dev:frontend-docker": "cd frontend && docker-compose -f docker-compose.dev.yml up",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npm run dev:backend & npm run dev:frontend",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0 <=20.x.x"
  },
  "keywords": [
    "strapi",
    "nextjs",
    "client-portal"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
