# Project
COMPOSE_PROJECT_NAME=bcflame

# Database
DB_USER=bcflame
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION
DB_NAME=bcflame_db
DB_PORT=5432

# Strapi
STRAPI_PORT=1337
JWT_SECRET=GENERATE_32_CHAR_SECRET
ADMIN_JWT_SECRET=GENERATE_32_CHAR_SECRET
APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=GENERATE_SECRET
TRANSFER_TOKEN_SALT=GENERATE_SECRET
SEED_DATA=true

# Frontend
FRONTEND_PORT=3000
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Email Configuration - Resend (Primary)
# Sign up at https://resend.com and get your API key
# Default from address is onboarding@resend.dev (no verification needed)
# To use a custom domain:
# 1. Go to https://resend.com/domains
# 2. Add your domain (e.g., bcflame.online)
# 3. Add DNS records (SPF, DKIM, DMARC)
# 4. Wait for verification (5-10 minutes)
# 5. Update RESEND_FROM_EMAIL below with your custom email
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=onboarding@resend.dev
# Custom domain example (uncomment after domain verification):
# RESEND_FROM_EMAIL=business@bcflame.online

EMAIL_FROM_NAME=BC Flame
EMAIL_ADMIN_RECIPIENTS=admin@bcflame.com

# SMTP Configuration (LEGACY - Optional, kept for backward compatibility)
# The system now uses Resend by default. SMTP is no longer required.
# For Gmail setup (if needed):
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to: Google Account → Security → 2-Step Verification → App passwords
# 3. Generate a new app password (16 characters)
# 4. Use the app password (no spaces) for SMTP_PASS below
# 5. Use your Gmail address for SMTP_USER
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
EMAIL_FROM_ADDRESS=noreply@bcflame.com
